#exec "systemctl --user import-environment {,WAYLAND_}DISPLAY SWAYSOCK; systemctl --user start sway-session.target"
exec systemctl --user import-environment DISPLAY WAYLAND_DISPLAY SWAYSOCK
exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec_always systemctl --user start sway-session.target

# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $mos Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:URWGothic-Book 9
titlebar_border_thickness 0
titlebar_padding 1

corner_radius 10
layer_effects "rofi" blur enable; shadows enable; corner_radius 25
layer_effects "notifications"; shadows enable;
layer_effects "waybar"; blur enable
titlebar_separator disable
blur enable
blur_radius 3
default_border pixel 0
default_floating_border pixel 0
shadows enable
# popup_during_fullscreen smart

input * {
    xkb_layout "tr"
    xkb_options "caps:escape"
}

input type:touchpad {
    tap enabled
    dwt enabled
}

# Adjust brightness
bindsym XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle
bindsym XF86Stop exec pactl set-sink-mute @DEFAULT_SINK@ toggle
bindsym XF86AudioMicMute exec pactl set-source-mute @DEFAULT_SOURCE@ toggle
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86Favorites exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86HOmePage exec playerctl next
bindsym XF86AudioPrev exec playerctl previous
bindsym XF86Search exec playerctl previous
bindsym --locked XF86AudioPlay exec playerctl play-pause
bindsym --locked XF86Favorites exec playerctl play-pause

set $WOBSOCK $XDG_RUNTIME_DIR/wob.sock
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +5% && pactl get-sink-volume @DEFAULT_SINK@ | head -n 1| awk '{print substr($5, 1, length($5)-1)}' > $WOBSOCK
bindsym XF86Forward exec pactl set-sink-volume @DEFAULT_SINK@ +5% && pactl get-sink-volume @DEFAULT_SINK@ | head -n 1| awk '{print substr($5, 1, length($5)-1)}' > $WOBSOCK
bindsym XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -5% && pactl get-sink-volume @DEFAULT_SINK@ | head -n 1 | awk '{print substr($5, 1, length($5)-1)}' > $WOBSOCK
bindsym XF86Back exec pactl set-sink-volume @DEFAULT_SINK@ -5% && pactl get-sink-volume @DEFAULT_SINK@ | head -n 1 | awk '{print substr($5, 1, length($5)-1)}' > $WOBSOCK

bindsym XF86MonBrightnessDown exec brightnessctl set 5%- | sed -En 's/.*\(([0-9]+)%\).*/\1/p' > $WOBSOCK
bindsym XF86MonBrightnessUp exec brightnessctl set +5% | sed -En 's/.*\(([0-9]+)%\).*/\1/p' > $WOBSOCK



# adjust volume
bindsym $mod+z exec zotero; [app_id="Zotero"] focus

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Shift+Return exec $term

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec exec dmenu_run
#
#wofi
bindsym $mod+p exec rofi -show
bindsym $mod+shift+p exec rofimoji
bindsym $mod+shift+c exec rofi -show calc -modi calc -no-show-match -no-sort -calc-command "echo -n '{result}' | wl-copy"
bindsym $mod+shift+v exec rofi -modi clipboard:cliphist-rofi-img.sh -show clipboard
bindsym $mod+d exec makoctl restore
bindsym $mod+shift+d exec makoctl dismiss
bindsym $mod+Ctrl+d exec makoctl menu fuzzel -d 


# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec i3-dmenu-desktop



#start some appilications
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+h focus right


# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+h move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+m split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# move the currently focused windo: to the scratchpad
bindsym $mod+Shift+minus move scratchpad
set $term kitty -1 --instance-group "kitty_sock" -o allow_remote_control=yes --listen-on unix:/tmp/kitty_sock
set $kw -o initial_window_width
set $kh -o initial_window_height

set $addterm $term --class term $kw=65c $kh=11c
set $addbtop $term --class btop $kw=50c $kh=20c -e btop
set $addncmp $term --class ncmpcpp -T ncmpcpp $kw=65c $kh=14c -e ncmpcpp
set $addclfm $term --class yazi -T yazi $kw=70c $kh=14c -e yazi 
#set $addcode $term --class rcode -T rcode  $kw=155c $kh=35c -e tmux new-session nvim
# set $addcode $term --class rcode -T rcode -d /home/samtrek/Documents/phd_thesis -e tmux new-session nvim
set $addcode $term --class rcode -T rcode -d /home/samtrek/Documents/phd_thesis -e nvim
set $addobsi $term --class obsiterm -T obsiterm  $kw=70c $kh=30c -d /home/samtrek/Documents/Obsidian\ Vault -e nvim
set $addchat $term --class nchat $kw=65c $kh=15c -e nchat
set $addthun thunar

# Show the next scratchpad window or hide the focused scratchpad window.
# If there are multiple scratchpad windows, this command cycles through them.
bindsym $mod+minus scratchpad show
#bindsym $mod+y exec ~/.config/sway/scripts/term.sh; [app_id="term"]scratchpad show
bindsym $mod+y exec swaymsg '[app_id="^term$"]scratchpad show' || $addterm && sleep .0001 && swaymsg '[app_id="^term$"]' move position 33ppt 0ppt
bindsym $mod+q exec swaymsg '[app_id="btop"]scratchpad show' || $addbtop && sleep .001 && swaymsg '[app_id="btop"]' move position 0ppt 58ppt
bindsym $mod+n exec swaymsg '[app_id="ncmpcpp"]scratchpad show' || $addncmp && sleep .001 && swaymsg '[app_id="ncmpcpp"]' move position 59ppt 71ppt
bindsym $mod+u exec swaymsg '[app_id="thunar"]scratchpad show' || $addthun && sleep .001 && swaymsg '[app_id="thunar"]' move position 55ppt 10ppt
bindsym $mod+o exec swaymsg '[app_id="yazi"]scratchpad show' || $addclfm && sleep .001 && swaymsg '[app_id="yazi"]' move position 55ppt 22ppt
bindsym $mod+b exec swaymsg '[app_id="obsiterm"]scratchpad show' || $addobsi && sleep .001 && swaymsg '[app_id="obsiterm"]' move position 0ppt 43ppt
bindsym $mod+c exec swaymsg '[app_id="rcode"] focus' || $addcode
bindsym $mod+a exec swaymsg '[app_id="nchat"]scratchpad show' || $addchat && sleep .001 && swaymsg '[app_id="nchat"]' move position 10ppt 0ppt

#bindsym $mod+u exec ~/.config/sway/scripts/thunar.sh; 


# toggle sticky
bindsym $mod+Shift+s sticky toggle

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"

# switch to workspace

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# bind some workspaces to a part of the window workspace $ws4 output DVI-I-1
workspace $ws1 output eDP-1 
workspace $ws2 output eDP-1
workspace $ws3 output eDP-1
workspace $ws4 output eDP-1
workspace $ws5 output HDMI-A-1
workspace $ws6 output HDMI-A-1
workspace $ws7 output HDMI-A-1
workspace $ws8 output HDMI-A-1

workspace $ws5 output DVI-I-1
workspace $ws6 output DVI-I-1
workspace $ws7 output DVI-I-1
workspace $ws8 output DVI-I-1
workspace $ws1 output DVI-I-2
workspace $ws2 output DVI-I-2
workspace $ws3 output DVI-I-2
workspace $ws4 output DVI-I-2 


# reload the configuration file
bindsym $mod+Shift+r reload 
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec nwg-bar
# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym scedilla resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

# Set inner/outer gaps
gaps inner 5

# Additionally, you can issue commands with the following syntax. This is useful to bind keys to changing the gap size.
# gaps inner|outer current|all set|plus|minus <px>
# gaps inner all set 10
# gaps outer all plus 5

# Smart gaps (gaps used if only more than one container on the workspace)
#smart_gaps on

# Smart borders (draw borders around container only if it is not the only container on this workspace) 
# on|no_gaps (on=always activate and no_gaps=only activate if the gap size to the edge of the screen is 0)
smart_borders on

# Press $mod+Shift+g to enter the gap mode. Choose o or i for modifying outer/inner gaps. Press one of + / - (in-/decrement for current workspace) or 0 (remove gaps for current workspace). If you also press Shift with these keys, the change will be global for all workspaces.
set $mode_gaps Gaps: (o) outer, (i) inner
set $mode_gaps_outer Outer Gaps: +|-|0 (local), Shift + +|-|0 (global)
set $mode_gaps_inner Inner Gaps: +|-|0 (local), Shift + +|-|0 (global)
bindsym $mod+Shift+g mode "$mode_gaps"

mode "$mode_gaps" {
        bindsym o      mode "$mode_gaps_outer"
        bindsym i      mode "$mode_gaps_inner"
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
mode "$mode_gaps_inner" {
        bindsym plus  gaps inner current plus 5
        bindsym minus gaps inner current minus 5
        bindsym 0     gaps inner current set 0

        bindsym Shift+plus  gaps inner all plus 5
        bindsym Shift+minus gaps inner all minus 5
        bindsym Shift+0     gaps inner all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}
mode "$mode_gaps_outer" {
        bindsym plus  gaps outer current plus 5
        bindsym minus gaps outer current minus 5
        bindsym 0     gaps outer current set 0

        bindsym Shift+plus  gaps outer all plus 5
        bindsym Shift+minus gaps outer all minus 5
        bindsym Shift+0     gaps outer all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}


# Open specific applications in floating mode
for_window [title="alsamixer"] floating enable border none
for_window [class="GParted"] floating enable border normal
for_window [class="qt5ct"] floating enable sticky enable border normal
for_window [app_id="^libreoffice"] border none
# for_window [app_id="Zotero"] floating enable
# for_window [app_id="Zotero" title = "^Zotero$"] floating disable
for_window [app_id="Zotero" title = "Progress"] floating enable, border none
for_window [app_id="Zotero" title = "Zotero Settings"] floating enable, border none
for_window [class="Pidgin"] floating enable, blur on, move position 10ppt 0ppt
for_window [class="Lxappearance"] floating enable, resize set width 60 ppt height 50 ppt
for_window [app_id="nchat"] floating enable, move position 10ppt 0ppt,resize set width 65ppt height 15ppt blur on
for_window [class="Stardict"] floating enable, opacity 0.95
for_window [app_id="org.keepassxc.KeePassXC"] floating enable
for_window [title="Syncthing-GTK"] floating enable
for_window [instance="Toplevel"] floating enable, focus
for_window [app_id="mpv"] floating enable
for_window [app_id="kitty"] focus
for_window [app_id="^kitty$|^term$|btop|ncmpcpp|yazi|^obsiterm$|nchat|rcode|rterm|foot"] opacity 0.90, blur enabled
for_window [app_id="^swayimg_"] floating enable, sticky toggle
for_window [app_id="^swayimg_"] resize set 197 125, move position 84ppt 1ppt
for_window [app_id="ncmpcpp"] floating enable; move scratchpad; scratchpad show; move position 59ppt 71ppt
for_window [app_id="rcode"] focus
# for_window [app_id="rcode"] floating enable, resize set width 100ppt height 65ppt,move position 0ppt 0ppt, focus
for_window [app_id="rterm"] floating enable,resize set width 40ppt height 34ppt, move position 60ppt 66ppt
for_window [app_id="^R$"] floating enable; blur enable,opacity 0.90, resize set width 683 304, move position 53ppt 1ppt
for_window [app_id="thunar"] floating enable; move scratchpad; scratchpad show; move position 55ppt 10ppt
for_window [app_id="yazi"] floating enable; move scratchpad; scratchpad show; move position 55ppt 22ppt
#for_window [instance="term"] move scratchpad
for_window [app_id="^term$"] floating enable;move scratchpad; scratchpad show; move position 33ppt 0ppt
for_window [app_id="^obsiterm$"] floating enable;move scratchpad; scratchpad show; move position 1ppt 43ppt
for_window [app_id="nchat"] floating enable;move scratchpad; scratchpad show; move position 10ppt 0ppt
for_window [app_id="btop"] floating enable; move scratchpad; scratchpad show; move position 0ppt 58ppt
for_window [title="Buddy List"] floating enable, 
for_window [app_id = "nm-connection-editor"] floating enable
for_window [app_id="^nwg"] floating enable, opacity 0.95
for_window [app_id="rstudio"] opacity 0.95, blur on
for_window [app_id="rstudio" title="RStudio: Notebook Output"] floating enable, opacity, 0.90 blur on
for_window [instance="r_x11"] floating enable, resize set 540 317, move position 1ppt 68ppt
for_window [app_id="notifications"] floating enable, opacity 0.95
for_window [app_id = "^blueman-manager$"] floating enable,  resize set width 40ppt height 30ppt
for_window [title="^About|^Library" app_id ="firefox"] floating enable
for_window [app_id="brave-ekhagklcjbdpajgpjgmbionohlpdbjgc-Default"] floating enable
for_window [title="^htop$"] floating enable
for_window [title="^Plot Zoom$"] floating enable
for_window [title="Preferences$"] floating enable
for_window [title= "^Extension: "] floating enable
# for_window [app_id ="Zotero" title="Quick Format Citation"] floating enabled, opacity 1, blur off, shadows off
for_window [app_id ="Zotero" title="Add/Edit Citation"] floating enabled
for_window [app_id ="Zotero" title="Add-ons Manager"] floating enable
for_window [title="Picture-in-Picture"] floating enable; sticky toggle
for_window [app_id="soffice"] floating enable
for_window [app_id="mainwindow.py"] floating enable
for_window [instance="winword.exe"] floating enable
for_window [app_id="file-roller"] floating enable
for_window [app_id="evince"] border none
for_window [app_id="udiskie"] floating enable
for_window [title="Picture-in-Picture"] resize set 197 125; move position 84ppt 1ppt 
for_window [window_role="task_dialog"] floating enable
for_window [window_role="pop_up"] floating enable
for_window [window_role="buddy_list"] floating enable
for_window [window_role="conversation"] floating enable, opacity 0.85, focus_on_window_activation none
for_window [window_type="dialog"] floating enable
for_window [window_type="menu"] floating enable
for_window [window_role="About"] floating enable
for_window [class="obsidian"] opacity 0.85, blur enabled, floating enabled
for_window [title="File Operation Progress"] floating enable, border pixel 1, sticky enable, resize set width 40ppt height 30ppt
for_window [title="Save File"] floating enable, border pixel 1, sticky enable, resize set width 40ppt height 30ppt
for_window [app_id="floating_shell_portrait"] floating enable, border pixel 1, sticky enable, resize set width 30ppt height 40ppt

assign [app_id="firefox"] number $ws1
assign [app_id="brave-browser"] number $ws1
assign [app_id="kitty"] number $ws4
assign [app_id="evince"] number $ws2
assign [app_id="Zotero" title ="^Zotero$"] number $ws3
assign [title="RStudio: Notebook Output"] number $ws4
assign [app_id="rcode|rterm"] number $ws6
assign [class="obsidian"] number $ws6
assign [app_id="^libreoffice"] number $ws5
assign [app_id="^rstudio"] number $ws7
assign [app_id="com.github.iwalton3.jellyfin-media-player" title = "Jellyfin Media Player"] number $ws8

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
 bar {
         swaybar_command waybar
         }

exec nm-applet
exec blueman-tray
exec gammastep
exec sleep 5 && $addchat
exec sleep 6 && swaymsg '[app_id="nchat"] scratchpad show'
exec udiskie -As
exec thunar --daemon
exec syncthing-gtk
exec blueman-applet
exec_always sworkstyle &> /tmp/sworkstyle.log
exec autotiling -l "2"
exec libinput-gestures
exec sway-prep-xwayland
exec ~/.config/sway/scripts/wallpaper.sh
exec kdeconnect-indicator
exec wl-paste --type text --watch cliphist store
exec wl-paste --type image --watch cliphist store
exec systemctl --user start xsettingsd.service
exec pomo.sh start
exec pomo.sh notify
exec gsettings set org.gnome.desktop.wm.preferences button-layout ":"
exec /usr/lib/geoclue-2.0/demos/agent


include /etc/sway/config.d/*
include ~/.config/sway/outputs
# Review the lines below and append to ~/.config/i3/config

